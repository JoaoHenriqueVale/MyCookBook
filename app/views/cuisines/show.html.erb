<h1><%=@cuisine.name%></h1>
<main>
<% if @cuisine.recipes.empty? %>
  <p>Nenhuma receita encontrada para este tipo de cozinha</p>

<% else %>
  <% @cuisine.recipes.each do |recipe| %>
    <%= render recipe %>
  <% end %>
<% end %>
</main>
<%= link_to 'Editar', edit_cuisine_path%>
<%= link_to 'Voltar', root_path%>
